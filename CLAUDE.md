# QA Pipeline - Human in the Loop

## 단축 명령어

사용자가 아래 숫자를 단독으로 입력하면, 해당 파일을 읽고 그 지시에 따라 실행한다.

| 입력 | 동작 |
|------|------|
| `1`  | `~/qa-pipeline_humanintheloop/instructions/step1_claude_code_검수포함.md` 파일을 읽고 실행 |
| `3`  | `~/qa-pipeline_humanintheloop/instructions/step3_playwright_검수포함.md` 파일을 읽고 실행 |
| `4`  | `~/qa-pipeline_humanintheloop/instructions/step4_claude_code_검수포함.md` 파일을 읽고 실행 |

> Step 2는 Manus에서 실행하므로 Claude Code에서는 제외.
